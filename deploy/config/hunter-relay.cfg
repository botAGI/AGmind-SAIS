global
  maxconn 64
  hard-stop-after 5s

defaults
  mode tcp
  timeout connect 3s
  timeout client 50s
  timeout server 50s

frontend core_to_hunter
  bind 0.0.0.0:8000
  default_backend exact_hunter

backend exact_hunter
  server hunter "${AGMIND_HUNTER_IPV4}:8000" check inter 5s fall 2 rise 2
